[build-system]
requires = ["setuptools>=80.3.1", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "splent_feature_comments"
version = "0.2.0"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [

]

[tool.setuptools]
package-dir = { "" = "src" }
include-package-data = true

[tool.setuptools.packages.find]
where = ["src"]
exclude = ["*.tests", "*.tests.*"]

[tool.splent]
cli_version = "1.11.0"

# ── Feature Contract (auto-generated) ────────────────────────────────────────
# Do not edit manually — re-run `splent feature:contract --write` to refresh.
# Preserved across refreshes: description, category, tags, env,
# requires.features_manual (dependencies invisible to static analysis)
# and provides.routes_manual (routes built from configuration at
# registration time, which have no literal path to find in source).
[tool.splent.contract]
description = "Visitor comments on posts with moderation (public form + admin approval)"
archetype = "full"

[tool.splent.contract.provides]
routes     = ["/admin/comments", "/admin/comments/<int:comment_id>/approve", "/admin/comments/<int:comment_id>/delete", "/comments/<int:post_id>"]
blueprints = []
models     = ["Comment"]
commands   = ["hello"]
hooks      = ["layout.authenticated_sidebar", "post.comments"]
services   = ["CommentsService"]
signals    = ["comment-created", "comment-submitting"]
translations = ["es"]
docker     = []

[tool.splent.contract.requires]
features = ["post"]
features_manual = ["post"]
env_vars = ["COMMENTS_AUTO_APPROVE"]
signals  = []

[tool.splent.contract.extensible]
services  = ["CommentsService"]
templates = ["comments/admin/list.html", "comments/index.html", "comments/thread.html"]
models    = ["Comment"]
hooks     = ["layout.authenticated_sidebar", "post.comments"]
routes    = false

# ── Configuration ─────────────────────────────────────────────────────────
[tool.splent.config]
# Publish new comments immediately instead of holding them for moderation.
# Also admin-editable in the settings panel, which wins at request time.
COMMENTS_AUTO_APPROVE = false
